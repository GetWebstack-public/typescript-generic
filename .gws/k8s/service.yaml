apiVersion: v1
kind: Service
metadata:
  name: typescript-generic
  namespace: typescript-generic
  labels:
    app: typescript-generic
spec:
  type: ClusterIP
  selector:
    app: typescript-generic
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
